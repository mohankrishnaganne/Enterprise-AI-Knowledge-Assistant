# =============================================================================
# Development and evaluation dependencies.
#
# Chains: requirements.txt (runtime) -> requirements-embeddings.txt (local
# encoder) -> this file (evaluation and test tooling). Streamlit Community Cloud
# installs only the first, which is why the chain is split at all. Install this one
# for local work, CI, or to regenerate the reports:
#
#     pip install -r requirements-dev.txt
# =============================================================================

-r requirements-embeddings.txt

# --- Evaluation ---------------------------------------------------------------
# ragas 0.4.x: `evaluate()` plus explicit LLM/embedding wrappers. Note it imports
# `langchain_community.chat_models.vertexai`, which langchain-community 0.4.x
# removed; src/evaluation/ragas_runner.py installs a stub for it before importing.
ragas==0.4.3
datasets==5.0.1
pandas==3.0.5

# --- Corpus generation --------------------------------------------------------
reportlab==5.0.1                 # renders the two sample PDFs in the corpus

# --- Testing and quality ------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.6                     # linter AND formatter (replaces black)
